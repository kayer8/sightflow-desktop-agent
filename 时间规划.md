# UI 等待时间规划

## 目标

微信和企业微信的真实 UI 操作等待统一经过 `randomizeUiWaitMs` 处理，降低固定节奏。

系统内部调度时间不参与随机化。以下时间保持原有逻辑：

- AI API 请求超时
- 截图、窗口查询、权限请求超时
- 窗口和截图缓存有效期
- 未读消息轮询间隔及 RuntimeHost 调度

## 随机函数

函数位置：`src/core/rpa/util.ts`

```ts
randomizeUiWaitMs(baseMs: number): number
```

计算规则：

1. 80% 概率生成 `0` 到 `2000ms` 的随机偏移量。
2. 20% 概率生成 `3000` 到 `5000ms` 的随机偏移量。
3. 偏移方向各 50% 概率：增加或减少。
4. 计算结果取绝对值。
5. 最终结果必须大于 `0ms`；结果为 `0` 时使用 `1ms`。
6. 返回整数毫秒。

## 已接入的 UI 等待

- 观察聊天窗口前等待
- 回复发送前等待
- 点击未读入口、联系人后的界面切换等待
- 鼠标移动、按压、点击后的等待
- 输入框聚焦、粘贴、回车发送过程中的等待

## 示例

传入 `1000ms`：

- 80% 分支：在 `1000ms` 附近增加或减少 `0-2000ms`
- 20% 分支：增加或减少 `3000-5000ms`

若计算结果为负数，先取绝对值；若结果为零，返回 `1ms`。
